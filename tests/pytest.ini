# content of pytest.ini
[pytest]
addopts = -s -v -rs --strict-markers
testpaths = tests
norecursedirs = build dist docs resources scripts src test_data
markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
    qualification: marks tests which are required for qualification (deselect with '-m "not qualification"')

